{% if subject.email is not null and subject.phone is not null %}
    <span class="c-value">{{ subject.email }}</span>
    <span class="c-sep" aria-hidden="true">·</span>
    <span class="c-value">{{ subject.phone }}</span>
{% endif %}
